app.controller('ProductCtrl', function($scope, OrderFactory) {

	$scope.addToCart = function(product) {
		alert('MAKE THIS FUNCTIONAL !');
		// OrderFactory.addToCart(product._id)
		// .then(function(cart) {
		// 	// possibly do something with cart
		// });
	};

});
